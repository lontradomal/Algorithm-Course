algoritmo "multiplication"
var
 count, n, ans : inteiro
inicio
   Count <- 1
    Escreva ("Type the number to count: ")
    Leia (N)
   Repita
        ans <- n * count
        Escreval (n, " X", count, " = ", ans)
        count <- count + 1
   ate (count > 10)
fimalgoritmo